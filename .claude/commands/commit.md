---
disable-model-invocation: true
---

`/commit` is retired — it knew one branch and no lanes, so "commit, merge and push" left every prototype and worktree to guesswork. Run the `/atelier-ship` procedure instead, passing the request through unchanged.

The user's request: $ARGUMENTS

Invoke `/atelier-ship` with the request above. If the request names only the current branch's changes, the ship plan simply contains that one commit and its push.
